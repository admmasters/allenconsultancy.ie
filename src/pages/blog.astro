---
import Layout from "../layouts/Layout.astro";
---

<Layout title="Blog">
	<div class="blog-container">
		<h1 class="text-focus-in">Blog</h1>
		
		<div class="coming-soon slide-in">
			<div class="coming-soon-content">
				<h2 class="color-shift">Blog Coming Soon</h2>
				<p class="fade-in-delay">
					I'm excited to launch my blog soon, where I'll be sharing insights, case studies, 
					and valuable information about child protection and safeguarding practices.
				</p>
				
				<div class="blog-topics flip-in">
					<h3>Topics I'll Be Covering:</h3>
					<ul class="item-animation">
						<li>Child protection best practices</li>
						<li>Risk assessment methodologies</li>
						<li>Protective capacity building for families</li>
						<li>Professional development for social workers</li>
						<li>Current research and innovations in child safeguarding</li>
					</ul>
				</div>
				
				<div class="notification bounce-in">
					<p>
						Subscribe to be notified when new blog posts are published:
					</p>
					<form class="subscribe-form">
						<input type="email" placeholder="Your email address" />
						<button type="submit" class="subscribe-button">Notify Me</button>
					</form>
				</div>
			</div>
		</div>
		
		<div class="cta swirl-in">
			<a href="/contact" class="cta-button">Suggest a Topic</a>
		</div>
	</div>
</Layout>

<style>
	.blog-container {
		padding: 2rem 0;
	}

	h1 {
		font-size: clamp(2.5rem, 5vw, 3.5rem);
		font-weight: 500;
		margin-bottom: 2.5rem;
	}

	.coming-soon {
		background-color: rgba(0, 0, 0, 0.3);
		border-radius: 8px;
		padding: 2.5rem;
		margin-bottom: 3rem;
		backdrop-filter: blur(5px);
	}

	.coming-soon-content {
		max-width: 800px;
		margin: 0 auto;
	}

	h2 {
		font-size: clamp(1.8rem, 3vw, 2.3rem);
		color: var(--color-primary);
		margin-bottom: 1.5rem;
		text-align: center;
	}

	h3 {
		font-size: clamp(1.2rem, 2vw, 1.5rem);
		margin-bottom: 1rem;
	}

	p {
		margin-bottom: 1.5rem;
		font-size: clamp(1rem, 1.2vw, 1.1rem);
		line-height: 1.6;
	}

	.blog-topics {
		margin: 2rem 0;
	}

	ul {
		list-style-position: inside;
		margin-left: 1rem;
	}

	li {
		margin-bottom: 0.5rem;
		font-size: clamp(1rem, 1.2vw, 1.1rem);
		opacity: 0.9;
	}

	.notification {
		background-color: rgba(230, 126, 34, 0.2);
		padding: 1.5rem;
		border-radius: 6px;
		margin-top: 2rem;
	}

	.notification p {
		margin-bottom: 1rem;
	}

	.subscribe-form {
		display: flex;
		gap: 1rem;
	}

	.subscribe-form input {
		flex: 1;
		padding: 0.75rem;
		border: none;
		border-radius: 4px;
		background-color: rgba(255, 255, 255, 0.9);
	}

	.subscribe-button {
		background-color: var(--color-primary);
		color: var(--color-text);
		border: none;
		padding: 0.75rem 1.5rem;
		border-radius: 4px;
		cursor: pointer;
		font-weight: 500;
		transition: background-color 0.2s;
	}

	.subscribe-button:hover {
		background-color: #d35400;
	}

	.cta {
		margin-top: 2rem;
		text-align: center;
	}

	.cta-button {
		display: inline-block;
		background-color: var(--color-primary);
		color: var(--color-text);
		text-decoration: none;
		padding: 0.75rem 1.5rem;
		border-radius: 4px;
		font-weight: 500;
		transition: background-color 0.2s;
	}

	.cta-button:hover {
		background-color: #d35400;
	}

	@media (max-width: 768px) {
		.blog-container {
			padding: 1rem 0;
		}
		
		.coming-soon {
			padding: 1.5rem;
		}
		
		.subscribe-form {
			flex-direction: column;
		}
		
		.subscribe-form input,
		.subscribe-button {
			width: 100%;
		}
		
		/* Adjust animation delays for mobile */
		.item-animation li:nth-child(1) { animation-delay: 0.15s; }
		.item-animation li:nth-child(2) { animation-delay: 0.18s; }
		.item-animation li:nth-child(3) { animation-delay: 0.21s; }
		.item-animation li:nth-child(4) { animation-delay: 0.24s; }
		.item-animation li:nth-child(5) { animation-delay: 0.27s; }
	}

	/* Animation styles */
	@keyframes textFocusIn {
		0% {
			filter: blur(12px);
			opacity: 0;
		}
		100% {
			filter: blur(0px);
			opacity: 1;
		}
	}

	@keyframes slideIn {
		0% {
			opacity: 0;
			transform: translateX(-100px);
		}
		100% {
			opacity: 1;
			transform: translateX(0);
		}
	}

	@keyframes colorShift {
		0% {
			opacity: 0;
			color: var(--color-text);
		}
		100% {
			opacity: 1;
			color: var(--color-primary);
		}
	}

	@keyframes fadeInDelay {
		0% {
			opacity: 0;
		}
		100% {
			opacity: 1;
		}
	}

	@keyframes flipIn {
		0% {
			opacity: 0;
			transform: rotateX(-90deg);
		}
		100% {
			opacity: 1;
			transform: rotateX(0);
		}
	}

	@keyframes bounceIn {
		0% {
			opacity: 0;
			transform: scale(0.3);
		}
		50% {
			opacity: 1;
			transform: scale(1.05);
		}
		70% {
			transform: scale(0.9);
		}
		100% {
			transform: scale(1);
		}
	}

	@keyframes swirlIn {
		0% {
			opacity: 0;
			transform: rotate(-540deg) scale(0);
		}
		100% {
			opacity: 1;
			transform: rotate(0) scale(1);
		}
	}

	.text-focus-in {
		animation: textFocusIn 0.4s cubic-bezier(0.550, 0.085, 0.680, 0.530) both;
	}

	.slide-in {
		opacity: 0;
		animation: slideIn 0.4s ease-out 0.05s forwards;
	}

	.color-shift {
		animation: colorShift 0.4s ease-out 0.1s forwards;
		opacity: 0;
	}

	.fade-in-delay {
		opacity: 0;
		animation: fadeInDelay 0.4s ease 0.15s forwards;
	}

	.flip-in {
		opacity: 0;
		transform-origin: top;
		animation: flipIn 0.4s ease-out 0.2s forwards;
	}

	.bounce-in {
		opacity: 0;
		animation: bounceIn 0.4s ease 0.25s forwards;
	}

	.swirl-in {
		opacity: 0;
		animation: swirlIn 0.4s ease-out 0.3s forwards;
	}

	.item-animation li {
		opacity: 0;
		transform: translateX(-20px);
		animation: fadeInDelay 0.4s ease forwards;
	}

	.item-animation li:nth-child(1) { animation-delay: 0.22s; }
	.item-animation li:nth-child(2) { animation-delay: 0.24s; }
	.item-animation li:nth-child(3) { animation-delay: 0.26s; }
	.item-animation li:nth-child(4) { animation-delay: 0.28s; }
	.item-animation li:nth-child(5) { animation-delay: 0.3s; }

	/* Interactive animations */
	.coming-soon {
		transition: transform 0.3s ease, box-shadow 0.3s ease;
	}

	.coming-soon:hover {
		transform: translateY(-5px);
		box-shadow: 0 10px 25px rgba(0, 0, 0, 0.3);
	}

	.subscribe-button {
		position: relative;
		overflow: hidden;
	}

	.subscribe-button::before {
		content: "";
		position: absolute;
		top: 0;
		left: -100%;
		width: 100%;
		height: 100%;
		background: linear-gradient(
			90deg,
			transparent,
			rgba(255, 255, 255, 0.2),
			transparent
		);
		transition: 0.3s;
	}

	.subscribe-button:hover::before {
		left: 100%;
	}
</style>
